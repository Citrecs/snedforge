// localStorage key + the current on-disk schema version. Bump the version (and add
// a migration) whenever the persisted shape changes.
export const STORAGE_KEY = 'learncard.history.v1';
export const CURRENT_SCHEMA_VERSION = 1;
